name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest
    
    steps:
    # - uses: docker://ros:kinetic
    - name: Run a multi-line script
      run: |
        ls
        # docker run --rm ros:kinetic ./build_ros1
