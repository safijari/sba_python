name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest
    
    steps:
    - uses: docker://ros:kinetic-ros-base:latest
    - name: Run a one-line script
      run: echo Hello, world!
    - name: Run a multi-line script
      run: |
        source /opt/ros/kinetic/setup.bash
        echo $ROS_DISTRO
